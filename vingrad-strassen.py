import numpy as np

def simple_miltiply(a, b):
    assert(a.shape[0] != b.shape[0] and a.shape[1] != b.shape[1])
    result = np.zeros(a.shape)
    for i in range(a.shape[0]):
        for j in range()
